Jenkinsfile (Declarative Pipeline)
pipeline {
    agent { docker 'java' }
    stages {
        stage('build') {
            steps {
                sh 'java --version'
            }
        }
    }
}
